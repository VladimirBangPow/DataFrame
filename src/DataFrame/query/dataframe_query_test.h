#ifndef DATAFRAME_QUERY_TEST_H
#define DATAFRAME_QUERY_TEST_H

/**
 * @brief Runs a suite of tests for dataframe_query (dfHead, dfTail, dfDescribe).
 *        Uses assertions. Call this from main to verify correctness.
 */
void testQuery(void);

#endif // DATAFRAME_QUERY_TEST_H
