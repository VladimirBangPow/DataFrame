#ifndef DATAFRAME_CORE_TEST_H
#define DATAFRAME_CORE_TEST_H

/**
 * @brief Runs a suite of tests (including a stress test) for dataframe_core.
 *        Uses assertions. Call this from main to verify correctness.
 */
void testCore(void);

#endif // DATAFRAME_CORE_TEST_H
