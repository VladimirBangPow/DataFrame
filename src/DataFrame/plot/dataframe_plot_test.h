#ifndef DATAFRAME_PLOT_TEST_H
#define DATAFRAME_PLOT_TEST_H

/**
 * @brief Runs a suite of tests (including line, scatter, hloc) for dataframe_plot (dfPlot).
 *        Uses assertions. Call this from main to verify correctness.
 */
void testPlot(void);

#endif // DATAFRAME_PLOT_TEST_H
