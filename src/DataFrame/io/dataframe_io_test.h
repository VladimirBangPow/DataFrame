#ifndef DATAFRAME_IO_TEST_H
#define DATAFRAME_IO_TEST_H

/**
 * @brief Runs a suite of tests (including some edge cases) for dataframe_csv (readCsv).
 *        Uses assertions. Call this from main to verify correctness.
 */
void testIO(void);

#endif // DATAFRAME_IO_TEST_H
